



- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>





- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>





- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>





- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>




- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>




- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>




- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>- Added new admin issue transfer/unassign flows, verification approval endpoint, and admin listing endpoint; updated audit log actions and OpenAPI registry.
- Overall: PR introduces multiple compile-time breaks (typos/missing imports), likely runtime route/controller wiring gaps, and a couple of authorization/logic inconsistencies.

<QODO_CONFIRM>
{"identifier":"sec-all","text":"1 Security vulnerability","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all security suggestions","type":"resolve_all","suggestionIdentifiers":["sec_001_permission_gap_issues_routes"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"sec_001_permission_gap_issues_routes","description":"**Admin issues endpoints appear to lack explicit permission gating**\n\n- Description: The issues routes expose claim/transfer/unassign operations but the diff shows no `requireAdminPermission(...)` middleware added for `/admin/issues`, so any authenticated admin may be able to reassign issues across departments/admins.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n router.post('/claim', adminIssuesController.claimIssue);\n@@\n router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n@@\n router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: In contrast, the verifications router explicitly adds `router.use(requireAdminPermission(['USER_MANAGEMENT']));`, indicating the codebase expects permission middleware for sensitive admin actions.\n- How to Fix: Add `requireAdminPermission([...])` (or equivalent) to the issues router (or at least to transfer/unassign endpoints) to enforce least privilege.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"high","prompt":"Add explicit permission middleware to admin issues routes (claim/transfer/unassign) consistent with other admin routers."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"bugs-all","text":"6 Potential bugs","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all bug suggestions","type":"resolve_all","suggestionIdentifiers":["bug_001_typo_listAvailableAdmins_call","bug_002_missing_imports_admin_verifications_controller","bug_003_missing_import_admin_role_admin_users_controller","bug_004_state_exports_controller_not_used_and_may_break_cycles","bug_005_route_breaking_change_transfer_return_paths","bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"bug_001_typo_listAvailableAdmins_call","description":"**Typo in service call will not compile (`l istAvailableAdmins`)**\n\n- Description: The controller calls `adminUsersService.l istAvailableAdmins(...)` with an embedded space, which is a syntax/identifier error and will fail TypeScript compilation.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n-    const admins = await adminUsersService.l istAvailableAdmins({ department });\n+    const admins = await adminUsersService.l istAvailableAdmins({ department });\n     const adminsSafe = admins.map(({ passwordHash, ...adminSafe }) => adminSafe);\n     new SuccessResponse('Available admins retrieved successfully', adminsSafe, 200).send(res);\n   });\n```\n- Evidence: `AdminUsersService` defines `async listAvailableAdmins(...)`, so the intended call is `adminUsersService.listAvailableAdmins(...)`.\n- How to Fix: Rename the call to `adminUsersService.listAvailableAdmins({ department })` (no space) and ensure the controller imports `adminUsersService`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Fix the typo in AdminUsersController: call adminUsersService.listAvailableAdmins(...) correctly and ensure imports are valid."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_002_missing_imports_admin_verifications_controller","description":"**`approveVerification` uses undeclared symbols (will not compile)**\n\n- Description: `approveVerification` references `AppError`, `AdminRole`, `AdminAuditCategory`, `adminAuditLogService`, and `workerProfileRepository` but the diff shows no corresponding imports added in `AdminVerificationsController.ts`.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n   approveVerification = asyncHandler(async (req, res): Promise<void> => {\n@@\n       if (!verification) throw new AppError('Verification not found', 404);\n@@\n     const adminRole = req.adminState.role as AdminRole;\n     await adminAuditLogService.record({\n@@\n       category: 'USER_MANAGEMENT' as AdminAuditCategory,\n```\n- Evidence: The only file in this PR that adds an `AppError` import is `AdminIssuesController.ts`, and no import changes are shown for `AdminVerificationsController.ts`.\n- How to Fix: Add the missing imports (or reference them via `state.ts` exports if that’s the pattern) so the controller compiles and uses the correct instances.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"high","prompt":"Add missing imports/DI wiring for approveVerification in AdminVerificationsController (AppError, AdminRole, AdminAuditCategory, adminAuditLogService, workerProfileRepository, etc.)."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_003_missing_import_admin_role_admin_users_controller","description":"**`AdminUsersController.getAvailableAdmins` uses `AdminRole` but diff shows no import**\n\n- Description: The new method casts `req.query.department as AdminRole` but the diff doesn’t add `AdminRole` import, which will fail compilation if it wasn’t already imported.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminUsersController.ts\n+++ b/src/controllers/AdminUsersController.ts\n@@\n   getAvailableAdmins = asyncHandler(async (req, res): Promise<void> => {\n     const department = req.query.department as AdminRole | undefined;\n```\n- Evidence: The PR adds `AdminRole` usage here, and the diff snippet contains no added import lines for `AdminRole` in this file.\n- How to Fix: Import `AdminRole` from `../domain/admin.entity.js` (or the project’s canonical location) in `AdminUsersController.ts`.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminUsersController.ts","severity":"high","prompt":"Ensure AdminUsersController imports AdminRole (and any other newly referenced types/services) for getAvailableAdmins."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_004_state_exports_controller_not_used_and_may_break_cycles","description":"**`state.ts` exports `adminUsersController` but routes likely instantiate/import controllers differently**\n\n- Description: `state.ts` now constructs `new AdminUsersController()` but the PR doesn’t show any router using `adminUsersController`, so this may be dead code or introduce circular dependency issues depending on how controllers/services are wired.\n- PR Git Diff Pointer:\n```diff\n--- a/src/state.ts\n+++ b/src/state.ts\n@@\n import AdminReportsController from './controllers/AdminReportsController.js';\n+import AdminUsersController from './controllers/AdminUsersController.js';\n@@\n export const adminUsersService = new AdminUsersService(adminRepository);\n+export const adminUsersController = new AdminUsersController();\n```\n- Evidence: The PR adds a new router mount `adminRouter.use('/admins', adminAdminsRouter);` but does not show any route file importing/using `adminUsersController`.\n- How to Fix: Either wire `adminUsersController.getAvailableAdmins` into the new `/admins` router (or existing router) or remove the unused export to avoid unintended initialization/cycles.","filePath":"d:\\projects Programming\\motqen-api\\src\\state.ts","severity":"medium","prompt":"Verify controller wiring: either use adminUsersController in routes or remove the unused export to avoid dead code/circular deps."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_005_route_breaking_change_transfer_return_paths","description":"**Breaking API change: renamed issue routes without backward compatibility**\n\n- Description: The endpoints `/admin/issues/transfer` and `/admin/issues/return` were replaced by `/transfer-admin`, `/transfer-department`, and `/unassign`, which will break existing clients unless aliases are kept.\n- PR Git Diff Pointer:\n```diff\n--- a/src/routes/v1/admin/issues.ts\n+++ b/src/routes/v1/admin/issues.ts\n@@\n-router.post('/transfer', adminIssuesController.transferIssue);\n+router.post('/transfer-admin', adminIssuesController.transferToAdmin);\n+router.post('/transfer-department', adminIssuesController.transferToDepartment);\n@@\n-router.post('/return', adminIssuesController.returnIssue);\n+router.post('/unassign', adminIssuesController.unassignIssue);\n```\n- Evidence: No compatibility routes (e.g., keeping `/transfer` and `/return` as aliases) are added in the diff.\n- How to Fix: Add deprecated aliases for the old paths (or bump API version and update all callers) to prevent immediate client breakage.","filePath":"d:\\projects Programming\\motqen-api\\src\\routes\\v1\\admin\\issues.ts","severity":"medium","prompt":"Add backward-compatible aliases for /admin/issues/transfer and /admin/issues/return or ensure all clients are updated and versioned."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"bug_006_transfer_to_admin_sets_department_to_target_role_including_super_admin","description":"**`transferToAdmin` may incorrectly change `assignedDepartment` when transferring to SUPER_ADMIN**\n\n- Description: `transferToAdmin` sets `assignedDepartment: targetAdmin.role`, so transferring to a `SUPER_ADMIN` will overwrite the issue’s department to `SUPER_ADMIN`, potentially losing the original department routing.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminIssuesService.ts\n+++ b/src/services/AdminIssuesService.ts\n@@\n     await this.updateIssue(params.targetType, params.targetId, {\n       assignedAdminId: params.newAdminId,\n-      assignedDepartment: targetAdmin.role,\n+      assignedDepartment: targetAdmin.role,\n     });\n```\n- Evidence: In `claimIssue`, the code explicitly preserves department for `SUPER_ADMIN` via `issue.assignedDepartment || 'SUPER_ADMIN'`, suggesting department is meaningful beyond the assignee.\n- How to Fix: Preserve `issue.assignedDepartment` when `targetAdmin.role === 'SUPER_ADMIN'` (or define explicit rules) instead of always overwriting with the target admin role.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminIssuesService.ts","severity":"medium","prompt":"Adjust transferToAdmin to avoid overwriting assignedDepartment to SUPER_ADMIN unless that is explicitly intended; preserve existing department when transferring to SUPER_ADMIN."}
</QODO_SUGGESTION>

<QODO_CONFIRM>
{"identifier":"quality-all","text":"2 Code quality issues","ctaText":"Resolve all","doneCtaText":"✓ All resolved","prompt":"Resolve all code quality suggestions","type":"resolve_all","suggestionIdentifiers":["qual_001_admin_users_service_uses_any_filter","qual_002_audit_metadata_inconsistency_null_vs_object"]}
</QODO_CONFIRM>

<QODO_SUGGESTION>
{"identifier":"qual_001_admin_users_service_uses_any_filter","description":"**`AdminUsersService.listAvailableAdmins` uses `any` for query filter**\n\n- Description: Using `const queryFilter: any` bypasses type safety and can hide invalid filter keys/values passed to `adminRepository.findMany`, leading to runtime query bugs.\n- PR Git Diff Pointer:\n```diff\n--- a/src/services/AdminUsersService.ts\n+++ b/src/services/AdminUsersService.ts\n@@\n   async listAvailableAdmins(filter?: { department?: AdminRole }): Promise<Admin[]> {\n-    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n+    const queryFilter: any = { status: 'ACTIVE' as AdminStatus };\n     if (filter?.department) {\n       queryFilter.role = filter.department;\n     }\n     return this.adminRepository.findMany({ filter: queryFilter });\n   }\n```\n- Evidence: The method returns `Promise<Admin[]>`, implying the repository layer is typed; `any` defeats that contract at the boundary.\n- How to Fix: Replace `any` with the repository’s expected filter type (or a typed DTO) so invalid filters are caught at compile time.","filePath":"d:\\projects Programming\\motqen-api\\src\\services\\AdminUsersService.ts","severity":"low","prompt":"Replace `any` queryFilter in listAvailableAdmins with a properly typed filter object matching adminRepository.findMany filter type."}
</QODO_SUGGESTION>

<QODO_SUGGESTION>
{"identifier":"qual_002_audit_metadata_inconsistency_null_vs_object","description":"**Inconsistent audit log metadata shape (`null` vs object) reduces observability**\n\n- Description: `approveVerification` records `metadata: null` while issue actions record structured metadata objects, making downstream audit log consumers handle multiple shapes and reducing queryability.\n- PR Git Diff Pointer:\n```diff\n--- a/src/controllers/AdminVerificationsController.ts\n+++ b/src/controllers/AdminVerificationsController.ts\n@@\n       targetType: 'VERIFICATION',\n       targetId: id,\n-      metadata: null,\n+      metadata: null,\n     });\n```\n- Evidence: In `AdminIssuesService`, new audit records include `metadata: { note: params.note }` and other structured fields, indicating metadata is intended for context.\n- How to Fix: Record a consistent metadata object (e.g., `{}` or `{ previousStatus: 'PENDING' }`) instead of `null` to simplify consumers.","filePath":"d:\\projects Programming\\motqen-api\\src\\controllers\\AdminVerificationsController.ts","severity":"low","prompt":"Standardize admin audit log metadata to a consistent object shape (avoid null) for verification approval records."}
</QODO_SUGGESTION>
